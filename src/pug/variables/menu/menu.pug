- let navClass = menu + ' ' + nav + ' ' + jsBurger
- let menuWrapper = menu + '__' + wrapper
- let menuInner = menu + '__' + inner
- let menuList = menu + '__' + list
- let navList = nav + '__' + list
- let navItem = menu + '__' + nav + '-' + item
- let navLink = menu + '__' + nav + '-' + link

-
  let navUlClasses= [
  menuList,
  navList
  ]

-
  let menuClasses= [
  // 0
  navClass,
  // 1
  menuWrapper,
  // 2
  menuInner,
  // 3
  navUlClasses,
  // 4
  navItem,
  // 5
  navLink
  ]

//- links
-
  let menuTextNames = [
    'Cтатьи',
    'Контакты',
    'О нас',
    'Контекстная реклама',
    'Создание сайтов',
  ]

- let urlIndex = urlPath[0] + urlLinkMenu[0]
- let urlArticles = urlPath[0] + urlLinkMenu[1]
- let urlCompany = urlPath[0] + urlLinkMenu[2]
- let urlMarketing = urlPath[0] + urlLinkMenu[3]
- let urlWebbuilding = urlPath[0] + urlLinkMenu[4]
- let urlContacts = urlPath[0] + urlLinkMenu[5]

-
  let menuLinksUrl = [
    urlIndex,
    urlArticles,
    urlCompany,
    urlMarketing,
    urlWebbuilding,
    urlContacts
  ]