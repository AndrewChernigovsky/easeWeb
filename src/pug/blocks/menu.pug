nav(class=menuClasses[0])
    div(class=menuClasses[1])
        include logo
    div(class=menuClasses[2])
        each ul in menuClasses
            if ul === navUlClasses
                ul(class=ul)
                    each val1 in menuItems
                        each val in val1
                            li(class=menuClasses[4])
                                +aLink(val, menuClasses[5], val)